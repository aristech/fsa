import { AISettingsView } from 'src/sections/settings/ai-settings/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'AI Settings',
};

export default function Page() {
  return <AISettingsView />;
}
